import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'allComponent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
